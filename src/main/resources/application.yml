micronaut:
  application:
    name: "micronautCafe"
datasources:
  default:
    url: "jdbc:postgresql://localhost:5432/postgres"
    driver: "lassName=org.postgresql.Driver"
    username: "postgres"
    password: "postgres"
    schema: "generate=CREATE_DROP"
    dialect: "POSTGRES"
